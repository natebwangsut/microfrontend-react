{
  "name": "microfrontend-react",
  "private": true,
  "workspaces": {
    "packages": [
      "body",
      "container",
      "header"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
